export const handler = async(event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Hello yml change from function1 folder!'),
    };
    return response;
};
